# 微信阅读色块对齐问题修复说明

## 问题描述

在微信阅读中使用"逐句色块"功能时，高亮色块的位置与实际文本不对应，出现明显的偏移。

## 根本原因

问题出现在 `electron/weread-preload.cjs` 文件的 `renderCanvasHighlights()` 函数中（第 1226-1281 行）。

### 技术细节

1. **Canvas 渲染架构**：微信阅读使用 Canvas 元素渲染文本，项目通过拦截 `fillText()` 调用来捕获文本位置信息。

2. **色块渲染方式**：
   - 当 Canvas 有透明背景时，使用 **underlay 层**（插入到 Canvas 元素之前的 `div`）
   - Underlay 层设置为 `position: absolute; inset: 0`，相对于**父容器**定位

3. **坐标系统不匹配**：
   - 捕获的文本坐标 (`entry.left`, `entry.top`) 是相对于 **Canvas 元素左上角**的 CSS 坐标
   - 但色块被放置在 underlay 层中，该层相对于 **Canvas 的父容器**定位
   - 如果 Canvas 在父容器中有偏移（margin、padding、或其他定位），色块就会错位

### 原始代码问题（第 1252-1264 行）

```javascript
if (underlay) {
  const fragment = underlayFragments.get(underlay) || document.createDocumentFragment();
  const block = document.createElement('span');
  block.className = active ? 'focus-reader-sentence-fragment is-active' : 'focus-reader-sentence-fragment';
  block.style.left = `${entry.left}px`;  // ❌ 直接使用相对于 Canvas 的坐标
  block.style.top = `${entry.top}px`;    // ❌ 没有考虑 Canvas 在父容器中的偏移
  block.style.width = `${entry.width}px`;
  block.style.height = `${entry.height}px`;
  block.style.background = color;
  fragment.append(block);
  underlayFragments.set(underlay, fragment);
  return;
}
```

## 修复方案

### 解决思路

计算 Canvas 相对于其父容器的偏移量，并将此偏移量加到色块坐标上。

### 修复代码

```javascript
function renderCanvasHighlights() {
  // ... 前面的代码 ...
  const canvasOffsets = new Map();  // ✅ 添加偏移量缓存
  
  canvasHighlightFragments(state.canvasSentences, state.activeSentence).forEach((entry) => {
    // ... 中间的代码 ...
    
    if (underlay) {
      // ✅ 计算 Canvas 相对于父容器的偏移
      let offset = canvasOffsets.get(sentence.canvas);
      if (!offset) {
        const container = sentence.canvas.parentElement;
        const canvasRect = sentence.canvas.getBoundingClientRect();
        const containerRect = container?.getBoundingClientRect();
        offset = {
          left: containerRect ? canvasRect.left - containerRect.left : 0,
          top: containerRect ? canvasRect.top - containerRect.top : 0,
        };
        canvasOffsets.set(sentence.canvas, offset);
      }
      
      const fragment = underlayFragments.get(underlay) || document.createDocumentFragment();
      const block = document.createElement('span');
      block.className = active ? 'focus-reader-sentence-fragment is-active' : 'focus-reader-sentence-fragment';
      block.style.left = `${entry.left + offset.left}px`;   // ✅ 加上偏移量
      block.style.top = `${entry.top + offset.top}px`;       // ✅ 加上偏移量
      block.style.width = `${entry.width}px`;
      block.style.height = `${entry.height}px`;
      block.style.background = color;
      fragment.append(block);
      underlayFragments.set(underlay, fragment);
      return;
    }
    // ... 后面的代码 ...
  });
}
```

### 关键改进

1. **添加偏移量缓存** (`canvasOffsets Map`)：避免重复计算同一 Canvas 的偏移量
2. **计算相对偏移**：使用 `getBoundingClientRect()` 获取 Canvas 和父容器的屏幕坐标，计算差值
3. **应用偏移量**：将偏移量加到色块的 `left` 和 `top` 坐标上

## 测试步骤

1. 重新构建项目：`npm run build`
2. 重新打包应用：`npm run package:windows`
3. 在微信阅读中测试：
   - 打开任意书籍
   - 启用"逐句色块"功能
   - 使用上下箭头键切换句子
   - 验证色块是否正确对齐文本

## 影响范围

- **修改文件**：`electron/weread-preload.cjs`
- **影响功能**：仅影响微信阅读中使用 Canvas 渲染且具有透明背景时的逐句高亮功能
- **向后兼容**：完全兼容，不影响其他阅读模式或 DOM 文本高亮

## 其他说明

- **Fallback 模式**：当 Canvas 背景不透明时，使用全局 fixed 定位的覆盖层，不受此问题影响
- **性能优化**：使用 Map 缓存偏移量计算结果，每个 Canvas 仅计算一次
- **边界情况处理**：当父容器不存在时，偏移量默认为 0，保持原有行为

## 版本信息

- 修复版本：1.4.9
- 修复日期：2026-07-29
- 相关文件：`electron/weread-preload.cjs` (第 1226-1281 行)
